% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/oregon_chm_downloader.R
\name{download_oregon_chm}
\alias{download_oregon_chm}
\title{Download Oregon Canopy Height Model (CHM) Data}
\usage{
download_oregon_chm(
  aoi,
  output_path = "oregon_lidar_output.tif",
  max_tile_size = 2000,
  mask = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{aoi}{sf or sfc object representing the area of interest}

\item{output_path}{Character string specifying the output file path}

\item{max_tile_size}{Numeric value specifying the maximum tile size in pixels}

\item{mask}{Logical indicating whether to mask the output to the AOI boundary}

\item{quiet}{Logical indicating whether to suppress progress messages}
}
\value{
Path to the downloaded and merged CHM raster file
}
\description{
Downloads CHM LiDAR data from DOGAMI's REST API service for a specified area of interest.
The Canopy Height Model represents the height of vegetation above ground level,
derived from the difference between Digital Surface Model (DSM) and Digital Terrain
Model (DTM).
}
\details{
The CHM data has a 3-meter resolution and represents vegetation height in meters.
Values typically range from 0 (ground level) to 60+ meters (tall trees).
The data is provided by Oregon Department of Geology and Mineral Industries (DOGAMI).
}
\examples{
\dontrun{
library(sf)
aoi <- st_read("path/to/aoi.gpkg")
# Download and mask to exact AOI boundary
chm_path <- download_oregon_chm(aoi, "chm_output.tif", mask = TRUE)
}
}
